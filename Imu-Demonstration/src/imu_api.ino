

/**
 * Public API for retrieving the x-coordinate of the acceleration from the IMU.
 *
 * @returns a float which represents the x-coordinate of the current acceleration
 * of the IMU.
 */
 /*
float getAccelerationX() {
    return extendedVals.aX;
}

/**
 * Public API for retrieving the y-coordinate of the acceleration from the IMU.
 *
 * @returns a float which represents the y-coordinate of the current acceleration
 * of the IMU.
 */
float getAccelerationY() {
    return extendedVals.aY;
}

/**
 * Public API for retrieving the z-coordinate of the acceleration from the IMU.
 *
 * @returns a float which represents the z-coordinate of the current acceleration
 * of the IMU.
 */
 /*
float getAccelerationZ() {
    return extendedVals.aZ;
}

/**
 * Public API for fetching the present yaw of the rocket.
 *
 * @returns a float which represents the yaw of the current positional state
 * of the IMU.
 */
 /*
float getYaw() {
    return currentVals.yaw;
}

/**
 * Public API for fetching the present pitch of the rocket.
 *
 * @returns a float which represents the pitch of the current positional state
 * of the IMU.
 */
 /*
float getPitch() {
    return currentVals.pitch;
}

/**
 * Public API for fetching the preset roll of the rocket.
 *
 * @returns a float which represents the roll of the current positional state
 * of the IMU.
 */
float getRoll() {
    return currentVals.roll;
}
